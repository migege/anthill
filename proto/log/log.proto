syntax = "proto3";

package migege.anthill.log;

service Logger {
    rpc Log(Info) returns (Info) {}
    rpc LogStatus(Info) returns (Info) {}
    rpc GetStatus(Info) returns (Info) {}
}

message Info {
    string info = 1;
    int64 ts = 2;
}
